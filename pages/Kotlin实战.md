type:: [[Book]]
category:: [[Kotlin]]
state:: reading

- 函数式编程
	- 核心概念
		- 头等函数
		- 不可变性
		- 无副作用
	- 好处
		- 简洁
		- 多线程安全
		- 测试更容易
- 目录和包
	- 不区分导入的是类还是函数
	- 星号导入不仅让包中定义的类可见，也会让顶层函数和属性可见
- `if` 和 `when` 都是有返回值的表达式
- 智能转换只在变量经过 `is` 检查且不再发生变化的情况下有效
- Kotlin的区间是包含的或者闭合的
-